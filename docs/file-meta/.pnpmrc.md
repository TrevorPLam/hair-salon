# File Meta: .pnpmrc

/\*\*

- @file .pnpmrc
- @role config
- @summary Sets pnpm install behavior for peer deps, hoisting, and lockfile enforcement.
-
- @entrypoints
- - pnpm CLI reads this on install and workspace operations
-
- @exports
- - N/A
-
- @depends_on
- - External: pnpm
-
- @used_by
- - CI install step in .github/workflows/ci.yml
- - Docker build in apps/web/Dockerfile (pnpm install)
-
- @runtime
- - environment: build
- - side_effects: affects dependency resolution and node_modules layout
-
- @data_flow
- - inputs: pnpm configuration flags in this file
- - outputs: resolved dependency graph and install behavior
-
- @invariants
- - strict-peer-dependencies=true enforces peer correctness
- - prefer-frozen-lockfile=true prevents lockfile drift in CI
-
- @gotchas
- - strict-peer-dependencies may break installs if peer deps are missing
-
- @issues
- - [severity:low] None observed in-file.
-
- @opportunities
- - Document any exceptions if future packages require hoisting
-
- @verification
- - Run: pnpm install and confirm no peer dependency errors
-
- @status
- - confidence: high
- - last_audited: 2026-02-09
    \*/

## Commentary

- This file is plain text; metaheader is stored here per project policy.
